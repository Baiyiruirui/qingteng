# 2026-07-14 全站体验一致性收口

## 本轮完成

- 字体固定为两级体系：中文无衬线承载操作信息，霞鹜文楷承载诗文、品牌与标题；表单控件继承同一字体。
- 聊天页移除独立 Header，统一接入 `AppNav`；四个主入口和退出登录在所有业务页保持一致。
- 新对话、展开原诗、结束沉浸改为适合窄屏的图标加渐进文本。
- 诗笺搜索词与朝代筛选同步到 URL；朗读、做题、沉浸入口携带 `returnTo`。
- 沉浸结束使用来源感知的 `router.replace`，避免结束后通过浏览器后退重新进入会话。
- 浏览器 `alert` 改成页面内错误提示；新增 `(app)/loading.tsx` 与 `(app)/error.tsx`。
- 待加强空状态增加“去诗笺地图”行动入口。

## 验证

- `pnpm build`：通过。
- 导航策略断言：4/4。
- `pnpm verify:security`：27/27，保留 `QT_ADMIN_USER_IDS` 未配置和本机 TLS 环境变量两个既有 warning。
- `pnpm verify:wrong-question`：5/5。
- `git diff --check`：通过。

## 未完成的视觉验证

应用内浏览器连接仍因 Windows `kernel assets` 写入权限失败，无法获取登录态桌面与移动端截图。因此本轮确认的是源码、响应式约束与生产构建，不宣称已经完成像素级截图验收。

## 下一批候选功能（遵守 CHARTER 既有范围）

1. 对话历史列表：复用现有 conversations API，补历史回看和续聊入口。
2. Playwright 主链路 smoke：登录、诗笺地图、出题、判题；章程已批准依赖。
3. Memory 查看与删除：明确隐私边界，也便于作品集演示“它记得什么”。
4. 性能优化包：字体子集化、诗笺分页或懒加载、Lighthouse 记录。
5. Token 成本视图：按用户和模块聚合 messages meta，展示工程成本意识。
