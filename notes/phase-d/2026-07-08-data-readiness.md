# Phase D-1 数据验收口径

**日期**: 2026-07-08
**阶段**: Phase D · 数据与工程 P0
**结论**: 基础内容和语义搜索已达到全库覆盖,沉浸与 v2 题库仍是精选深做。

## 当前口径

- 基础诗词内容: 140 首,全部有结构化诗句。
- 语义诗词搜索: 140 首已生成 bge-m3 embedding,并有 HNSW 索引。
- 腾讯 ASR 朗读评分: 功能可覆盖全库,音频不落盘,评分结果写入 `events(type='recite')`。
- 诗境沉浸: 精选 3 首有手工脚本,用于 demo 深度体验。
- 青藤考你 v2: 20 道蓝图题,覆盖 3 首诗,`evidenceValid` 目标为 100%。

## 本次发现

- 历史待办里提到的 `dynasty = Tang` 在数据库统计中已经不再出现。
- 源数据 `data/poems/TANG_016.json` 仍保留 `"Tang"`,会导致后续重导入或对外展示口径不一致。
- 原有 `db:verify` 适合快速看统计,但不能作为上线前验收红线。

## 本次处理

- 新增 `pnpm verify:data`,同时检查源 JSON 与数据库覆盖。
- 将 `data/poems/TANG_016.json` 的朝代从 `Tang` 修为 `唐`。
- 把 140 首全库覆盖和 3 首精选深做拆成两个口径,避免汇报时混淆。
- 顺手移除 `next/font/google` 依赖,避免生产构建时因 Google Fonts 网络请求失败而中断。

## 后续

- Phase D 继续清 P0 时,优先处理 Memory 膨胀治理与沉浸模式记忆提取。
- 如果要把题库从精选 3 首扩到 140 首,需要单独开蓝图规模化任务,并保留人工抽检 10% 的验收步骤。
