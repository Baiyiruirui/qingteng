# 2026-07-15 · 会话历史与 Playwright smoke

## 本轮完成

- `/api/conversations` 增加登录态 GET，只返回当前用户最近会话的标题、预览、模式、诗名和消息数。
- `/chat` 增加「旧日对话」抽屉，支持当前会话标识、移动端全宽、桌面窄抽屉、Esc/遮罩关闭和旧会话续聊。
- 不修改数据库 schema；标题优先取首条用户消息，预览取最新消息，空白会话不进入历史列表。
- 新增 Playwright 配置、公开入口 smoke、登录后历史抽屉 smoke，以及诗笺地图 → 做题 → 先生批注主链路 smoke。
- 判题 e2e 使用浏览器层 mock，避免模型成本和答题数据污染。

## 验证结果

- `pnpm build`：通过。
- `pnpm verify:security`：28/28 通过；保留 `QT_ADMIN_USER_IDS` 未配置与本机 TLS 环境两项 warning。
- `pnpm test:e2e`：公开入口 1 passed；登录后 2 skipped，因为本机尚未配置专用 E2E 账号。
- `pnpm exec tsc --noEmit`：通过。

## 下一次执行

在 PowerShell 临时设置 `E2E_USER_NAME`、`E2E_USER_PASSWORD` 后运行 `pnpm test:e2e`，完成登录后两条 smoke 的首次实跑。测试账号应与个人演示账号分开，凭据不得写入文件或提交。
