# Phase A-2 全站视觉走查

日期：2026-07-13  
实现提交：`695cdd7`

## 目标

完成做题页“先生批注”细节，并确认核心路径在桌面端与移动端没有明显的溢出、遮挡、层级混乱或视觉语言断裂。

## 覆盖范围

- 登录态：`/chat`、`/poems`、`/wrong`、`/profile`、`/recite`、`/recite/[poemId]`
- 做题链路：题目态、主观题判题结果态、移动结果态
- 未登录态：`/login`、`/register`
- 视口：桌面 `1440 x 1000`，移动 `390 x 844`

共检查 19 个页面状态，自动扫描结果为 0 个横向溢出页面；`pnpm build` 通过。

## 本轮修正

1. 做题结果从通用卡片改为“先生批注”：突出掌握度、已命中得分点、可补要点、青藤反馈和参考答案。
2. 统一做题页、待加强页和诗笺地图的 `paper / ink / jade / cinnabar` token，移除局部旧色值写法。
3. 全局导航在移动端重排为两行，保留当前页标题、四个核心入口和用户信息。
4. 今日案头输入区在案头态回归正常文档流，只在真实对话态吸底，避免移动端遮挡诗笺。
5. 待加强页补齐专项复习的行动层级，并区分“待加强”和“已掌握”。

## 走查结论

- 做题链路已经能直接呈现 LLM-as-judge 的产品价值，不再只显示对错。
- 桌面与移动端的导航、正文、按钮和长文本均可正常容纳。
- 登录、诗笺地图、今日案头、待加强、学习画像和朗读页视觉语言一致。
- 浏览器整页截图会把 `sticky` 导航拼接在长图中部；真实视口内导航固定在顶部，不构成页面元素重叠。

Phase A-2 视觉与讲点尾项至此关闭，后续进入 Phase E 发布材料与密钥轮换。
